#
# Copyright (c) 2024 - 2025 Ping Identity Corporation. All rights reserved.
#
# This software may be modified and distributed under the terms
# of the MIT license. See the LICENSE file for details.
#

name: Prepare Device Farm Artifacts
on:
  workflow_call:
    inputs:
      head_sha:
        description: Head commit SHA to check out
        type: string
      head_repo:
        description: Head repository to check out
        type: string
    secrets:
      SIGNING_KEYSTORE:
        description: 'Needed for signing the apk artifacts'
        required: true
      SIGNING_ALIAS:
        description: 'Needed for signing the apk artifacts'
        required: true
      SIGNING_KEYSTORE_PASSWORD:
        description: 'Needed for signing the apk artifacts'
        required: true
      SIGNING_KEY_PASSWORD:
        description: 'Needed for signing the apk artifacts'
        required: true
      SLACK_WEBHOOK:
        description: Slack Notifier Incoming Webhook
        required: true
      E2E_CONFIG:
        description: 'Variables for the e2e tests'
        required: true
      DAVINCI_E2E_CONFIG:
        description: 'Variables for the DaVinci e2e tests'
        required: true
jobs:
  prepare-device-farm-artifacts:
    runs-on: ubuntu-latest

    steps:
      # Clone the repo
      - name: Clone the repository
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.head_sha || github.event.pull_request.head.sha }}
          repository: ${{ inputs.head_repo || github.event.pull_request.head.repo.full_name }}
          persist-credentials: false

      # Set up the variables for the tests from the secrets
      - name: Setup config file for e2e tests
        run: |
          echo "${{ secrets.E2E_CONFIG }}" > journey/src/main/assets/test_config.properties
          echo "${{ secrets.DAVINCI_E2E_CONFIG }}" > davinci/src/main/assets/davinci_test_config.properties

      # Setup JDK and cache and restore dependencies.
      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      # Build apk files
      - name: Prepare device farm artifacts
        run: |
          ./gradlew assembleDebug assembleDebugAndroidTest \
            --stacktrace \
            --build-cache \
            --parallel

      # List the available build tools versions see https://github.com/r0adkll/sign-android-release/issues/84
      - name: List build tools versions
        run: ls $ANDROID_HOME/build-tools/

      # Sign pingsampleapp-debug.apk
      - name: Sign pingsampleapp-debug.apk
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: samples/pingsampleapp/build/outputs/apk/debug
          signingKeyBase64: ${{ secrets.SIGNING_KEYSTORE }}
          alias: ${{ secrets.SIGNING_ALIAS }}
          keyStorePassword: ${{ secrets.SIGNING_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.SIGNING_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "35.0.0"

      # Rename to the canonical name expected by downstream run workflows
      - name: Rename pingsampleapp-debug-signed.apk to app-debug-signed.apk
        run: mv samples/pingsampleapp/build/outputs/apk/debug/pingsampleapp-debug-signed.apk samples/pingsampleapp/build/outputs/apk/debug/app-debug-signed.apk

      # Sign davinci-debug-androidTest.apk
      - name: Sign davinci-debug-androidTest.apk
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: davinci/build/outputs/apk/androidTest/debug
          signingKeyBase64: ${{ secrets.SIGNING_KEYSTORE }}
          alias: ${{ secrets.SIGNING_ALIAS }}
          keyStorePassword: ${{ secrets.SIGNING_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.SIGNING_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "35.0.0"

      # Sign journey-debug-androidTest.apk
      - name: Sign journey-debug-androidTest.apk
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: journey/build/outputs/apk/androidTest/debug
          signingKeyBase64: ${{ secrets.SIGNING_KEYSTORE }}
          alias: ${{ secrets.SIGNING_ALIAS }}
          keyStorePassword: ${{ secrets.SIGNING_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.SIGNING_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "35.0.0"

      # Sign oath-debug-androidTest.apk
      - name: Sign oath-debug-androidTest.apk
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: mfa/oath/build/outputs/apk/androidTest/debug
          signingKeyBase64: ${{ secrets.SIGNING_KEYSTORE }}
          alias: ${{ secrets.SIGNING_ALIAS }}
          keyStorePassword: ${{ secrets.SIGNING_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.SIGNING_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "35.0.0"

      # Sign push-debug-androidTest.apk
      - name: Sign push-debug-androidTest.apk
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: mfa/push/build/outputs/apk/androidTest/debug
          signingKeyBase64: ${{ secrets.SIGNING_KEYSTORE }}
          alias: ${{ secrets.SIGNING_ALIAS }}
          keyStorePassword: ${{ secrets.SIGNING_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.SIGNING_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "35.0.0"

      # Publish the signed APKs as build artifacts
      - name: Publish app-debug-signed.apk
        uses: actions/upload-artifact@v7
        if: success()
        with:
          name: app-debug-signed.apk
          path: samples/pingsampleapp/build/outputs/apk/debug/app-debug-signed.apk

      - name: Publish davinci-debug-androidTest.apk
        uses: actions/upload-artifact@v7
        if: success()
        with:
          name: davinci-debug-androidTest-signed.apk
          path: davinci/build/outputs/apk/androidTest/debug/davinci-debug-androidTest-signed.apk

      - name: Publish journey-debug-androidTest.apk
        uses: actions/upload-artifact@v7
        if: success()
        with:
          name: journey-debug-androidTest-signed.apk
          path: journey/build/outputs/apk/androidTest/debug/journey-debug-androidTest-signed.apk

      - name: Publish oath-debug-androidTest.apk
        uses: actions/upload-artifact@v7
        if: success()
        with:
          name: oath-debug-androidTest-signed.apk
          path: mfa/oath/build/outputs/apk/androidTest/debug/oath-debug-androidTest-signed.apk

      - name: Publish push-debug-androidTest.apk
        uses: actions/upload-artifact@v7
        if: success()
        with:
          name: push-debug-androidTest-signed.apk
          path: mfa/push/build/outputs/apk/androidTest/debug/push-debug-androidTest-signed.apk

      # Send slack notification ONLY if any of the steps above fail
      - name: Send slack notification
        uses: 8398a7/action-slack@v3
        with:
          status: custom
          fields: all
          custom_payload: |
            {
              attachments: [{
                title: ':no_entry: Failed to prepare BrowserStack test artifacts',
                color: 'danger',
                text: `\nWorkflow: ${process.env.AS_WORKFLOW} -> ${process.env.AS_JOB}\nPull request: ${process.env.AS_PULL_REQUEST}\nCommit: ${process.env.AS_COMMIT} by ${process.env.AS_AUTHOR}`, 
              }]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
        if: failure()